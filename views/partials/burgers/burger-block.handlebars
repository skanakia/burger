<li class="burgerLi">
	{{burger_name}}

	<button class="Devoured" data-id="{{id}}" data-neweaten="{{devoured}}">
		{{#if devour}}Devour!{{else}}Regurgitate!{{/if}}
	</button>

	<button class="delete" data-id="{{id}}">
		Delete
	</button>
</li>
